{
  "name": "jujutsu-brawl-online",
  "version": "1.0.0",
  "description": "Jujutsu Brawl with online multiplayer WebSocket room support.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.cjs",
    "check": "node --check game.js && node --check visuals.js && node --check animations.js && node --check server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  }
}
